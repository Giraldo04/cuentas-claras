Form field for text, dates and non-hero numbers (the quick-entry amount uses Keypad instead).

```jsx
<Input label="Nombre del bolsillo" placeholder="Viaje a Cartagena" iconLeft="target" />
```

48px tall, 12px radius, sunken surface. Pass `error` for the invalid state — never rely on colour alone, the message text carries the meaning.
