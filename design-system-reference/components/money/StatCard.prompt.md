KPI tile — 3–4 across the top of a dashboard or a PDF report block.

```jsx
<StatCard label="Ingresos" value={6200000} tone="income" icon="trending-up" delta="Igual que en julio" />
```

24px tabular figure, 11px uppercase label, 12px radius, 1px border, no shadow (matches the PDF spec).
