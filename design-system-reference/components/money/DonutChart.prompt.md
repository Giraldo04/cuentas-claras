Category breakdown. Rounded caps, no gridlines, no legend block — label the data directly around it.

```jsx
<DonutChart segments={[{value:40,color:'var(--cc-expense)'},{value:25,color:'var(--cc-savings)'}]}
  centerValue={<Amount value={3120000} size={22} />} centerLabel="Gasto del mes" />
```
