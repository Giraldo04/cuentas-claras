One card per financial commitment; the same component serves loan, instalment plan, mortgage and trip.

```jsx
<CommitmentCard kind="hipoteca" name="Apartamento" monthly={1840000} paid={18} total={240} balance={318000000}
  expanded={open} onToggle={toggle}><Escalera months={months} /></CommitmentCard>
```
