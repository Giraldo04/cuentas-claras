Icon-only control for toolbars and card corners; always pass `label`.

```jsx
<IconButton name="bell" label="Notificaciones" tone="surface" />
```

Default size is 44px so it meets the tap-target floor.
