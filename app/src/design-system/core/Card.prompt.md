The container every grouped block sits in — 1px warm border, 16px radius, soft shadow.

```jsx
<Card padding="var(--cc-space-5)"><h3 className="cc-display">Este mes</h3></Card>
```

Keep elevation at `xs` in lists; reserve `md`/`lg` for sheets and floating surfaces.
