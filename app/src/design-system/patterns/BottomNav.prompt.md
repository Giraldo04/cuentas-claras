The app's only global navigation. Five slots, fixed order, central FAB opens quick entry.

```jsx
<BottomNav value={tab} onChange={setTab} onFab={() => setSheet(true)} />
```

Never add a sixth item or reorder the five.
