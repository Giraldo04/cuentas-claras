A savings envelope ("bolsillo") with its own emoji, colour and goal.

```jsx
<PocketCard emoji="✈️" name="Viaje a Cartagena" saved={1850000} goal={4000000} />
```

Percentage is shown as a number and a bar — never as colour alone.
