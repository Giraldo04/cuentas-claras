Household member identity token — initials on a tinted disc.

```jsx
<MemberAvatar name="Ana Ruiz" color="var(--cc-savings)" selected />
<MemberAvatar name="Tomás" color="var(--cc-ant)" isChild />
```

Dashed ring = child (spend destination). Solid ring + halo = selected.
